bool KingdomHearts(){
    function();
    function(id, x, c);
    int b,c,d,e,f,g,h;
    bool k = "hola";
    bool k = (3 > 5);
    int x = 1;
    char a = '2';
    munny q = 12.12;
    KChain asd = "v";
    int b,c,d,e,f,g,h;
    switch(k){
        case 1:{
            if ( true || popeye && (5 + 3 == 6)){

            }
            ragnarok;
        }
    }

    if ( true || popeye && (3 + 5 != 6 > 3)){
        if (k == 3){
            while (k && (popeye)){
                while (kill && (ID > ID) ){
                    k = k + 1;
                    for (munny i = 0; k < 3; k = k + 1){
                        print(popeye);
                        print("HOLA POPEYE COELLO");
                        print("saoidhisad\d\c", id,k);
                        scan(ID);
                    }
                }
            }
        }
    }
}

void popeye (int x, bool y, munny z, KChain a){
     if ( true || popeye && (3 + 5 != 6 > 3)){
        if (k == 3){
            while (k && (popeye)){
                while (kill && (ID > ID) ){
                    k = k + 1;
                    for (munny i = 0; k < 3; k = k + 1){
                        print(popeye);
                        print("HOLA POPEYE COELLO");
                        print("saoidhisad\d\c", id,k);
                        scan(ID);
                    }
                }
            }
        }
    }
}

bool popeye2 (int x, bool y, char k){
     if ( true || popeye && (3 + 5 != 6 > 3)){
        if (k == -3){
            while (k && (popeye)){
                while (kill && (ID > ID) ){
                    k = k + 1;
                    for (munny i = 0; k < 3; k = k + 1){
                        print(a);
                        print("HOLA POPEYE COELLO");
                        print("saoidhisad\d\c", id,k);
                        scan(ID);
                    }
                }
            }
        }
    }
}